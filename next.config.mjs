/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['unsplash.com', 'images.unsplash.com', 'lh3.googleusercontent.com', 'ui-avatars.com'],
    },
};

export default nextConfig;
